const config = {
branches: ['main'],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/git",{
     "assets":["dist/*.py", "dist/*.py".map"],
    "messege": "chore(release): ${NEXTRelease.version} [skip.ci]\n\n{next}
}],
    "@semantic-release/github"
  ]
},
